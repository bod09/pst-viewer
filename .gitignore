node_modules
dist
dist-ssr
deploy
*.local
.DS_Store
.vite
# Local sample PST/OST/zip test fixtures (never commit private mail data)
/fixtures
*.pst
*.ost
*.msg
*.eml
# Fidelity baselines describe a real mailbox (subjects, senders): keep them local
/.fidelity
